# Python backend (vendor-lookup-rag)

Package source: flat `src/` (import name is still `vendor_lookup_rag`; see `package-dir` in `pyproject.toml`). Install with `pip install -e .` from this directory.

The Streamlit UI lives under `../../frontend/streamlit/`; see the repository root [README.md](../../README.md).
